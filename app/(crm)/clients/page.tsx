import Link from 'next/link'
import { Search, Upload, Plus, Phone, ChevronRight } from 'lucide-react'
import { telHref } from '@/lib/validators'
import { createClient } from '@/lib/supabase/server'
export const dynamic='force-dynamic'
export default async function ClientsPage(){const supabase=await createClient();const {data=[],count=0}=await supabase.from('clients').select('*',{count:'exact'}).order('created_at',{ascending:false});return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 className="text-2xl font-black">Clients</h1><p className="mt-1 text-sm text-slate-500">{count??0} contacts depuis Supabase.</p></div><div className="flex gap-2"><button className="btn btn-ghost text-sm"><Upload size={16}/>Importer CSV</button><button className="btn btn-primary text-sm"><Plus size={16}/>Nouveau client</button></div></div><section className="card overflow-hidden"><div className="flex border-b border-slate-100 p-4"><div className="relative flex-1"><Search size={17} className="absolute left-3 top-3 text-slate-400"/><input placeholder="Rechercher un nom, téléphone ou ville" className="w-full rounded-xl border border-slate-200 py-2.5 pl-10 pr-3 text-sm outline-none focus:border-amber-400"/></div></div><div className="divide-y divide-slate-100">{data.map(client=><div key={client.id} className="flex flex-col gap-4 p-4 hover:bg-amber-50/40 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-full bg-amber-100 font-black text-amber-800">{client.first_name[0]}{client.last_name[0]}</div><div><Link href={`/clients/${client.id}`} className="font-bold hover:text-amber-700">{client.first_name} {client.last_name}</Link><p className="text-xs text-slate-500">{client.city} · {client.phone}</p></div></div><div className="flex items-center gap-3"><a href={telHref(client.phone)} className="btn btn-primary text-sm"><Phone size={15}/>Appeler</a><ChevronRight size={18} className="text-slate-400"/></div></div>)}</div></section></div>}
