import { Sidebar } from '@/components/crm/sidebar'
import { MobileNav } from '@/components/crm/mobile-nav'
import { Bell, Menu, ShieldCheck } from 'lucide-react'
import { getCurrentRole } from '@/lib/admin-auth'
export default async function CrmLayout({children}:{children:React.ReactNode}){ const role=await getCurrentRole()||'agent'; return <div className="flex min-h-screen bg-[#edf1f4]"><Sidebar role={role}/><div className="min-w-0 flex-1 pb-20 lg:pb-0"><header className="flex h-12 items-center justify-between bg-[#242424] px-4 text-white shadow-sm"><div className="flex items-center gap-3"><Menu size={17} className="text-slate-300"/><span className="text-xs font-semibold text-slate-300">Centre d’appel AtlasMiel</span></div><div className="flex items-center gap-4"><Bell size={16} className="text-slate-300"/><span className="hidden items-center gap-1.5 text-[11px] font-semibold text-emerald-400 sm:flex"><ShieldCheck size={14}/>{role==='admin'?'Administrateur':role==='supervisor'?'Superviseur':'Agent'}</span><div className="flex items-center gap-2 border-l border-white/10 pl-4"><div className="grid h-7 w-7 place-items-center rounded-full bg-[#17a589] text-[10px] font-black">AM</div><span className="hidden text-xs sm:inline">AtlasMiel</span></div></div></header><main className="p-3 sm:p-4 lg:p-5">{children}</main></div><MobileNav role={role}/></div> }
