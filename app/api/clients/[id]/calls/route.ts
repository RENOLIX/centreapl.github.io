import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { callOutcomeSchema } from '@/lib/validators'
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const parsed=callOutcomeSchema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:'Résultat invalide'},{status:400});const supabase=await createClient();const {data:auth}=await supabase.auth.getUser();if(!auth.user)return NextResponse.json({error:'Connexion requise'},{status:401});const {data,error}=await supabase.rpc('complete_agent_call',{p_client_id:id,p_result_id:parsed.data.resultId,p_duration_seconds:parsed.data.durationSeconds,p_summary:parsed.data.summary||''});return NextResponse.json(error?{error:error.message}:{id:data},{status:error?409:201})}
