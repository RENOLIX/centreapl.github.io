import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { callOutcomeSchema } from '@/lib/validators'
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const parsed=callOutcomeSchema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:'Résultat invalide'},{status:400});const supabase=await createClient();const {data:auth}=await supabase.auth.getUser();if(!auth.user)return NextResponse.json({error:'Connexion requise'},{status:401});const {data:agent}=await supabase.from('agents').select('id').eq('user_id',auth.user.id).maybeSingle();if(!agent)return NextResponse.json({error:'Ce compte n’est pas un agent actif'},{status:403});const {data,error}=await supabase.from('calls').insert({client_id:id,agent_id:agent.id,result_id:parsed.data.resultId,duration_seconds:parsed.data.durationSeconds,summary:parsed.data.summary||''}).select().single();return NextResponse.json(data||{error:error?.message},{status:error?500:201})}
