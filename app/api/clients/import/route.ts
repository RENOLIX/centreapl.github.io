import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { parseClientRows } from '@/lib/csv'
export async function POST(request:Request){const body=await request.json() as {rows:Record<string,string>[]};const parsed=parseClientRows(body.rows||[]);if(parsed.accepted.length){const supabase=await createClient();const rows=parsed.accepted.map(client=>({first_name:client.firstName,last_name:client.lastName,phone:client.phone,email:client.email,city:client.city,metadata:{telephone_2:client.phone2,adresse:client.address,commune:client.commune,wilaya:client.wilaya,total:client.total,produit:client.product}}));const {error}=await supabase.from('clients').insert(rows);if(error)return NextResponse.json({error:error.message,rejected:parsed.rejected},{status:500})}return NextResponse.json(parsed)}
