'use client'
import { FormEvent,useState } from 'react'
import { useRouter } from 'next/navigation'
import { Phone,CheckCircle2 } from 'lucide-react'

type Result={id:string;label:string}
export function AgentWorkstation({client,results}:{client:{id:string;first_name:string;last_name:string;phone:string;email:string;city:string;metadata:Record<string,unknown>;campaign_name:string|null;campaign_script:string|null};results:Result[]}){const router=useRouter();const [message,setMessage]=useState('');const [called,setCalled]=useState(false);const labels:Record<string,string>={telephone_2:'Téléphone 2',adresse:'Adresse',commune:'Commune',wilaya:'Wilaya',total:'Total',produit:'Produit',information:'Informations'};const details=Object.entries(client.metadata||{}).filter(([,value])=>value!==''&&value!=null);async function finish(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const response=await fetch(`/api/clients/${client.id}/calls`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({resultId:form.get('resultId'),durationSeconds:form.get('durationSeconds'),summary:form.get('summary')})});const body=await response.json();if(!response.ok)return setMessage(body.error||'Enregistrement impossible');setMessage('Statut enregistré. Chargement du prochain client…');router.refresh()}return <div className="grid gap-4 xl:grid-cols-[1fr_360px]"><section className="card overflow-hidden"><div className="bg-[#25292d] px-5 py-3 text-xs font-bold uppercase tracking-wide text-white">{client.campaign_name||'Campagne active'}</div><div className="p-6"><p className="text-xs uppercase text-slate-400">Client en cours</p><h2 className="mt-2 text-3xl font-black">{client.first_name} {client.last_name}</h2><div className="mt-6 grid gap-3 sm:grid-cols-2"><div className="border border-slate-200 p-4"><p className="text-[10px] uppercase text-slate-400">Téléphone</p><p className="mt-1 text-xl font-bold">{client.phone}</p></div><div className="border border-slate-200 p-4"><p className="text-[10px] uppercase text-slate-400">Ville</p><p className="mt-1 text-lg font-bold">{client.city||'Non renseignée'}</p></div></div>{details.length>0&&<div className="mt-4 grid gap-3 sm:grid-cols-2">{details.map(([key,value])=><div key={key} className="rounded-lg bg-slate-50 p-3"><p className="text-[10px] font-bold uppercase text-slate-400">{labels[key]||key}</p><p className="mt-1 text-sm font-semibold">{String(value)}</p></div>)}</div>}{client.campaign_script&&<div className="mt-5 border-l-4 border-cyan-500 bg-cyan-50 p-4"><p className="text-xs font-black uppercase text-cyan-800">Script</p><p className="mt-2 whitespace-pre-wrap text-sm leading-6 text-slate-700">{client.campaign_script}</p></div>}<a onClick={()=>setCalled(true)} href={`tel:${client.phone.replace(/[^\d+]/g,'')}`} className="btn btn-primary mt-6 text-base"><Phone size={18}/>Appeler manuellement</a></div></section><form onSubmit={finish} className="card p-5"><div className="flex items-center gap-2 font-black"><CheckCircle2 className="text-emerald-600"/>Terminer ce client</div><p className="mt-1 text-xs text-slate-500">Après le statut, le prochain client sera affiché automatiquement.</p><select name="resultId" required className="mt-5 w-full border border-slate-200 p-3"><option value="">Statut de l’appel</option>{results.map(result=><option key={result.id} value={result.id}>{result.label}</option>)}</select><input name="durationSeconds" type="number" min="0" placeholder="Durée en secondes" className="mt-3 w-full border border-slate-200 p-3"/><textarea name="summary" placeholder="Résumé / note" className="mt-3 min-h-28 w-full border border-slate-200 p-3"/><button disabled={!called} className="btn btn-primary mt-3 w-full justify-center disabled:cursor-not-allowed disabled:opacity-40">Enregistrer et client suivant</button>{!called&&<p className="mt-2 text-center text-[10px] text-slate-400">Cliquez d’abord sur Appeler.</p>}{message&&<p className="mt-3 text-sm font-bold text-amber-700">{message}</p>}</form></div>}
