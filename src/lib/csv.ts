export type CsvClient = { firstName:string; lastName:string; phone:string; email:string; city:string }
const aliases:Record<string,string[]>={firstName:['prenom','prénom','firstname','first_name','nom'],lastName:['nom de famille','lastname','last_name'],phone:['telephone','téléphone','phone','mobile'],email:['email','e-mail','mail'],city:['ville','city']}
function key(value:string){return value.trim().toLowerCase().replace(/\s+/g,' ')}
export function parseClientRows(rows:Record<string,string>[]){const accepted:CsvClient[]=[];const rejected:{row:number;reason:string}[]=[];const seen=new Set<string>(); rows.forEach((row,index)=>{const normalized=Object.fromEntries(Object.entries(row).map(([k,v])=>[key(k),String(v??'').trim()])); const get=(field:string)=>{const found=aliases[field].find(a=>normalized[a]);return found?normalized[found]:''}; const phone=get('phone'); const firstName=get('firstName'); const lastName=get('lastName')||'Client'; if(!phone||!firstName){rejected.push({row:index+2,reason:'Prénom et téléphone obligatoires'});return} const dedupe=phone.replace(/\D/g,'');if(seen.has(dedupe)){rejected.push({row:index+2,reason:'Téléphone en doublon'});return}seen.add(dedupe);accepted.push({firstName,lastName,phone,email:get('email'),city:get('city')})}); return {accepted,rejected} }
